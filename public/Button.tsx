import React from "react";
import './Button.scss';

export interface ButtonProps {
  id?: string;
  // Write your props here
}

export const Button = ({
  id, ...props
}: ButtonProps) => {

  return (
    <div>
      
    </div>
  );
}