import React from "react";
import './<%= name %>.scss';

export interface <%= name %>Props {
  id?: string;
  // Write your props here
}

export const <%= name %> = ({
  id, ...props
}: <%= name %>Props) => {

  return (
    <div>
      
    </div>
  );
}